<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartvendingmachine">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- 인터넷 사용 권한 (서버 접속하려면 필수) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"> <!-- 모든 Http URL에 대해서 접근 허용 (서버 접속하려면 필수) -->

        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.board.BoardActivity" />    <!-- 게시글 Activity -->
        <activity android:name=".ui.board.BoardWriteActivity"/> <!-- 게시글 작성 Activity -->

    </application>

</manifest>